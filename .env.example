# Supabase (projet public : clé anon uniquement, jamais la service_role côté client)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Si vide, l'application bascule automatiquement sur les jeux de données mock typés.
